/**
 * Sometimes you need to override wrong Typings, it should be avoided, but if
 * you want to not override lib definitions and create additional typings, you
 * can do it here.
 */

export default null;
